# CLA signatures

This directory holds CLA signature records written by the CLA GitHub Action
(`.github/workflows/cla.yml`). Do not create `v1/cla.json` by hand — the
workflow creates it on the first valid signature.

See [CLA.md](../../CLA.md) for the agreement text.
