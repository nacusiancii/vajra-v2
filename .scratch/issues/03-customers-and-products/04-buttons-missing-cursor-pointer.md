Status: needs-triage

## Bug: Buttons in Customer and Product pages do not have cursor pointer

Action buttons (Add, Edit, Delete) do not show a pointer cursor on hover. This is a basic affordance cue — clickable elements should show `cursor: pointer`. May be a shadcn-vue Button variant issue or a missing CSS rule.
