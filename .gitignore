# Archived previous iteration - kept locally for reference, not tracked
archive/

# Old local design scratchpad (removed; do not reintroduce)
.scratch/

# Cloned skills repo - vendored locally, not tracked
matt-pocock-skills/

# Node
node_modules/
.pnpm-store/

# Build output
dist/
out/
build/

# Local env / secrets (never commit)
.env
.env.*
!.env.example

# Local databases (app data lives outside the repo; belt-and-braces)
*.db
*.db-journal
*.sqlite
*.sqlite3

# Claude Code local-only settings (per-machine approvals etc.)
.claude/settings.local.json

# Test artifacts
test-results/
playwright-report/

# Editor / OS
.DS_Store
*.log
.eslintcache
